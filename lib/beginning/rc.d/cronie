#!/bin/bash

cronie_exists() {
    type -fPp crond
}

cronie_start() {
    cronie_status || rm -f /var/run/crond.pid
    crond -n "${CRONIE_ARGS}" &
    echo $! > /var/run/crond.pid
}

cronie_stop() {
    kill $(</var/run/crond.pid)
    cronie_status || rm -f /var/run/crond.pid
}

cronie_status() {
    pidexists /var/run/crond.pid
}

